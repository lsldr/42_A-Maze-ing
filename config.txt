#required by project file (this are mandatory)
WIDTH=10				# int
HEIGHT=10				# int
ENTRY=1,1				# tuple x,y where x < WIDTH and y < HEIGHT
EXIT=9,9			# like ENTRY
OUTPUT_FILE=maze.txt
#default: False
PERFECT=False			# if maze should have only one path (true) or more

# seed for maze gen, can be int, none or just ommited
# to use default
# defailt: none
SEED=42

# algorithm for maze gen
# avalible: dfs, wilson, irk
# default: dfs
MAZEGEN=wilson

# pathfinding algorithm
# avalible: bfs, astar
# default: bfs
# PATHFINDING=astar
