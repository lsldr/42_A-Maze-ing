#required by project file (this are mandatory)
WIDTH=20				# int
HEIGHT=15				# int
ENTRY=6,5				# tuple x,y where x<WIDTH and y<HEIGHT
EXIT=18,14				# like ENTRY
OUTPUT_FILE=maze.txt
#default: False
PERFECT=True			# if maze should have only one path (true) or more (false)