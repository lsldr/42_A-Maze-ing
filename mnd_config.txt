#required by project file (this are mandatory)
WIDTH=50				# int
HEIGHT=50				# int
ENTRY=1,3				# tuple x,y where x<WIDTH and y<HEIGHT
EXIT=45,42				# like ENTRY
OUTPUT_FILE=maze.txt
#default: False
PERFECT=False			# if maze should have only one path (true) or more (false)